#ifndef _sub_c_
#define _sub_c_

#include <stddef.h>
#include <stdlib.h>
#include <string.h>

unsigned char *sub_digits(
    unsigned char *a, size_t la,
    unsigned char *b, size_t lb,
    size_t *lr);

#endif
